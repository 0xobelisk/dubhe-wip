  // Copyright (c) Obelisk Labs, Inc.
  // SPDX-License-Identifier: Apache-2.0
  #[allow(unused_use)]
  
  /* Autogenerated file. Do not edit manually. */
  
  module test_project::test_project_monster_catch_result {

  use sui::bcs::{to_bytes};

  use dubhe::table_id;

  use dubhe::dapp_state;

  use dubhe::dapp_service;

  use dubhe::dapp_hub;

  use dubhe::dapp_hub::DappHub;

  use test_project::test_project_dapp_key;

  use test_project::test_project_dapp_key::DappKey;

  use test_project::test_project_status;

  use test_project::test_project_status::{Status};

  const TABLE_NAME: vector<u8> = b"monster_catch_result";

  public fun get_table_id(): vector<u8> {
    table_id::encode(table_id::onchain_table_type(), TABLE_NAME)
  }

  public fun get_key_schemas(): vector<vector<u8>> {
    vector[b"address"]
  }

  public fun get_value_schemas(): vector<vector<u8>> {
    vector[b"Status"]
  }

  public fun get_key_names(): vector<vector<u8>> {
    vector[b"monster"]
  }

  public fun get_value_names(): vector<vector<u8>> {
    vector[b"result"]
  }

  public fun register_table(dapp_hub: &mut DappHub, ctx: &mut TxContext) {
    let dapp_key = test_project_dapp_key::new();
    dapp_service::register_table(
            dapp_hub, 
            dapp_key,
            get_table_id(), 
            TABLE_NAME, 
            get_key_schemas(), 
            get_key_names(), 
            get_value_schemas(), 
            get_value_names(), 
            ctx
        );
  }

  public fun has(dapp_hub: &DappHub, monster: address): bool {
    let mut key_tuple = vector::empty();
    key_tuple.push_back(to_bytes(&monster));
    dapp_service::has_record<DappKey>(dapp_hub, get_table_id(), key_tuple)
  }

  public fun ensure_has(dapp_hub: &DappHub, monster: address) {
    let mut key_tuple = vector::empty();
    key_tuple.push_back(to_bytes(&monster));
    dapp_service::ensure_has_record<DappKey>(dapp_hub, get_table_id(), key_tuple)
  }

  public fun ensure_not_has(dapp_hub: &DappHub, monster: address) {
    let mut key_tuple = vector::empty();
    key_tuple.push_back(to_bytes(&monster));
    dapp_service::ensure_not_has_record<DappKey>(dapp_hub, get_table_id(), key_tuple)
  }

  public fun delete(dapp_hub: &mut DappHub, monster: address) {
    let mut key_tuple = vector::empty();
    key_tuple.push_back(to_bytes(&monster));
    dapp_service::delete_record<DappKey>(dapp_hub, test_project_dapp_key::new(), get_table_id(), key_tuple);
  }

  public fun get(dapp_hub: &DappHub, monster: address): Status {
    let mut key_tuple = vector::empty();
    key_tuple.push_back(to_bytes(&monster));
    let value = dapp_service::get_field<DappKey>(dapp_hub, get_table_id(), key_tuple, 0);
    let mut bsc_type = sui::bcs::new(value);
    let value = test_project::test_project_status::decode(&mut bsc_type);
    value
  }

  public fun set(dapp_hub: &mut DappHub, monster: address, value: Status) {
    let mut key_tuple = vector::empty();
    key_tuple.push_back(to_bytes(&monster));
    let value_tuple = encode(value);
    dapp_service::set_record(dapp_hub, test_project_dapp_key::new(), get_table_id(), key_tuple, value_tuple);
  }

  public fun encode(value: Status): vector<vector<u8>> {
    let mut value_tuple = vector::empty();
    value_tuple.push_back(test_project::test_project_status::encode(value));
    value_tuple
  }
}
